# LunaAiCut AI 剪辑预告片：风格预设与 Agent 开发路线图

更新时间：2026-08-17

文档状态：产品与工程设计稿 + 当前实现记录

当前纯提示词初版已整理到 [内置剪辑技能目录](packages/freecut-editor/src/features/ai-editing/skills/built-in/README.md)。其中 `luna-editing-master` 是总控技能，`styles/*` 是可组合的风格技能。它们已经通过 FreeCut Harness 插件注册到运行时；当前阶段仍只使用提示词，不包含 TTS、节拍分析等新增模型工具。

运行时接入链路：

```text
skills/built-in/**/*.md
        ↓ build:harness-runtime
dist/deepseek-harness/skills/built-in
        ↓ luna-freecut-plugin.mjs
ctx.skills.register()
        ↓ Harness tool-skill
技能目录 → 模型按需加载大师技能和风格技能
```

## 当前进程记录

记录时间：2026-08-17

### 已完成

- 建立 `built-in` 内置技能目录。
- 建立 `luna-editing-master` AI 剪辑大师总控技能。
- 建立 6 个可组合的风格技能：旅行 Vlog、电影感纪实、快节奏 / 卡点、家庭纪录片、口播 / 讲解、情绪化蒙太奇。
- 建立创作需求简报和 AI 剪辑执行契约两个共享提示词资源。
- 新增加载器 `scripts/deepseek-harness-built-in-skills.mjs`，支持源码目录和打包目录。
- FreeCut Harness 插件启动时通过 `ctx.skills.register()` 注册 7 个技能。
- 大师技能加载时自动合并两个共享提示词；风格技能通过 Harness `skill` 工具按需加载。
- 构建流程会将提示词资源复制到 `dist/deepseek-harness/skills/built-in`，安装包可以携带这些资源。
- 已更新本路线图和内置技能目录说明。

### 当前已经能工作的行为

- Harness 技能目录可以发现 1 个大师技能和 6 个风格技能。
- 模型可以加载大师技能，再根据任务加载一个或多个风格技能。
- 风格技能只提供镜头、节奏、声音、字幕和画面策略，真正的时间轴修改仍由 FreeCut 结构化工具执行。
- 用户消息仍由 Harness 原样传递，宿主不根据关键词猜测用户意图或风格。
- 大师技能明确要求基于素材证据工作；没有证据时不能声称识别了人物、场景、情绪或节拍。

### 当前尚未实现

- 尚未把风格提示词转换为 `StyleManifest` 等结构化风格协议；当前先使用 skill 直接指导基础工具调用。
- 尚未增加 `style.*`、`edit.plan`、`edit.apply_plan` 和 `edit.diff` 工具；基础 `media.*` / `timeline.*` 工具已足够支撑第一版剪辑。
- 尚未完成镜头切分、场景 / 人物 / 质量 / 音频 / 节拍分析索引。
- 尚未接入 TTS 试听、生成、授权记录和配音时间轴工具。
- 尚未实现多个风格版本并存、版本切换和用户自定义风格包。
- 尚未实现 Asset Scout、Audio Planner、Caption Designer、Style Critic 等多 Agent 协作流程。

### 已完成验收

- `pnpm run test:deepseek-harness` 通过。
- `pnpm run build:harness-runtime` 通过。
- 源码目录和 `dist/deepseek-harness` 打包目录均已成功读取 7 个技能。
- 真实 Harness `SkillRegistry` 已验证 7 个技能可注册、可发现、可加载；大师技能包含两个共享契约。
- `node --check` 和 `git diff --check` 通过。

### 下次继续的位置

下一步使用现有 `media.*` / `timeline.*` 工具进行真实素材验收，重点优化 skill 的剪辑顺序和响应速度；暂不增加新的宿主工具、结构化计划协议或 TTS 能力。

## 1. 这次预告片真正要交付什么

预告片的核心不是“增加一个聊天窗口”，而是让用户用一句自然语言描述想要的作品，LunaAiCut 就能把这句话转换成一套可执行的创作方案，并真正修改时间轴。

目标体验是：

```text
用户表达想要的感觉
        ↓
Agent 理解创作意图，补齐必要参数
        ↓
选择或组合剪辑风格预设
        ↓
分析素材、声音和用户偏好
        ↓
生成镜头结构、节奏、字幕和音乐方案
        ↓
调用结构化时间轴工具完成剪辑
        ↓
用户继续用自然语言提出修改
```

“旅行 Vlog”“电影感”“节奏快一点”“家庭纪录片”“口播”“卡点”“情绪化蒙太奇”不应只是几段提示词。它们要成为可组合、可检查、可调节、可扩展的风格技能（skill）。

预告片中的“AI 记住你”也不应等同于把全部聊天记录塞给模型，而应当是：AI 在用户明确确认后，保存可复用的剪辑偏好，并在下一次创作时作为默认倾向参与规划。

## 2. 当前实现能直接复用什么

当前项目已经有一条可以承接风格系统的底座，不需要重做 Harness：

| 当前能力 | 现状 | 对后续风格系统的意义 |
| --- | --- | --- |
| DeepSeek Harness Web runtime | 通过 `electron/deepseekHarnessService.ts` 启动，FreeCut 通过插件接入工具和系统提示词 | 继续作为主 Agent、会话、工具循环和 Agent UI 的宿主 |
| FreeCut 工具插件 | `scripts/deepseek-harness-freecut-plugin.mjs` 注册当前 28 个工具 | 在现有结构上增加风格、分析、配音和计划工具 |
| 素材读取 | `media.list`、`media.read`、`media.analyze`、`media.search_transcript` | 先用已有证据完成第一版风格预设，再逐步增加镜头、人物、声音等分析 |
| 时间轴编辑 | `project.*`、`timeline.*` 已支持加入、裁剪、分割、移动、删除、文字、音频、关键帧、转场和批量操作 | 第一版风格预设可以直接通过这些工具落地 |
| 编辑器命令层 | `packages/freecut-editor/src/features/timeline/stores/timeline-command-store.ts` 已有事务和历史基础 | 只作为工具执行时的最低安全保障，不把它包装成面向用户的主要产品能力 |
| 用户记忆 | `memory.read`、`memory.search`、`memory.update`、`memory.remove` 已接入 Electron 用户数据目录 | 可保存用户长期偏好，但要与风格预设和项目规则分开 |
| 工具结果回传 | 工具结果返回 Harness，由模型决定下一步 | 必须保持，宿主不能根据用户或模型文案猜测意图、风格或任务完成状态 |
| Agent / 子 Agent 机制 | Harness 已提供 `ctx.agents`、Agent preset 和子 Agent 相关包；FreeCut 插件已经注入 `agents`、`agentPresets` | 可以增加素材分析员、节奏分析员、字幕设计员等只读角色 |

当前底座还不能兑现预告片的全部表述。特别是“识别人、场景、镜头和声音”“根据音乐卡点”“用户自定义风格”“文本生成语音”都需要新增能力和结构化协议。

## 3. 产品核心：风格预设不是 Prompt，而是创作策略

### 3.1 风格预设的职责

每个风格预设都要回答以下问题：

1. 这类视频通常如何开场、推进、转折和收尾。
2. 什么样的镜头值得保留，什么样的镜头应当降低优先级。
3. 镜头平均持续多久，哪些时刻允许突然加速或留白。
4. 如何处理原声、环境声、音乐、口播和配音之间的关系。
5. 字幕采用什么密度、位置、字号和动画方式。
6. 画幅、构图、转场、运镜、色彩和速度调整的默认取值是什么。
7. 哪些参数必须询问用户，哪些参数可以使用默认值。
8. 怎样验证最终结果确实符合这个风格。

因此，风格预设的执行结果不是一段解释，而是一份结构化的剪辑策略：镜头选择规则、节奏规则、声音规则、文字规则、画面规则和质量检查规则。

### 3.2 风格预设的统一数据结构

第一版建议使用声明式 JSON 或 YAML，禁止用户自定义包直接执行 JavaScript、Shell 或任意宿主代码。

```yaml
id: style.travel-vlog
version: 1.0.0
name: 旅行 Vlog
scope: built-in
description: 轻快、真实、突出地点和行程变化的旅行短片

inputs:
  required:
    - media
  optional:
    - duration
    - aspectRatio
    - music
    - voiceover
    - userPreferences

story:
  structure:
    - establish
    - discover
    - highlight
    - close
  preferredShotRoles:
    - establishing
    - action
    - detail
    - humanReaction
    - landscape
  avoid:
    - duplicateShots
    - unusableFrames
    - repeatedStaticAngles

rhythm:
  targetCutSeconds: [0.8, 3.5]
  allowLongShotSeconds: 6
  useBeatGridWhenMusicExists: true
  preserveQuietMoments: medium

visual:
  preferredAspectRatios: ["9:16", "16:9"]
  motion: light
  transitionDensity: low
  colorDirection: natural-bright

audio:
  preserveAmbientSound: true
  musicLevelDb: -18
  voiceLevelDb: -6
  duckMusicUnderVoice: true

captions:
  enabledByDefault: false
  maxCharactersPerLine: 16
  position: bottom-safe
  stylePresetId: clean-white

quality:
  requireOpeningWithinSeconds: 2
  requireEndingWithinSeconds: 2
  maxRepeatedMediaRatio: 0.2
  checks:
    - storyCoverage
    - audioClipping
    - captionSafeArea
    - emptyGap
```

这份结构是给模型、工具和测试使用的内部协议。用户界面可以把它呈现成“节奏、画面、声音、字幕、情绪”等可调整的控件，不需要让用户看到技术字段。

### 3.3 风格参数分层

风格参数分为四层，避免“用户一句话改了一个参数，整个风格被重做”：

| 层级 | 例子 | 生命周期 |
| --- | --- | --- |
| 本轮创作要求 | 这条视频 30 秒、做成 9:16、开头直接进入重点 | 只影响当前任务，不自动保存 |
| 当前项目规则 | 这个项目保留孩子的原声，不使用夸张转场 | 由项目规则管理 |
| 视频类型偏好 | 家庭视频尽量保留完整动作和自然停顿 | 可作为视频类型记忆 |
| 用户长期偏好 | 我喜欢字幕靠下、音乐不要盖住人声 | 只有用户明确确认后保存 |

冲突顺序继续沿用当前项目约定：本轮用户要求 > 项目规则 > 用户长期偏好 > 风格默认值。

## 4. 第一批内置风格预设

第一批不追求数量，先做 6 至 8 个具有明显差异、可以通过测试区分的预设。

### 4.1 旅行 Vlog

- 故事：地点建立、行程推进、人物反应、当地细节、记忆点收尾。
- 镜头：远景建立环境，中景交代行动，近景补充食物、手部、路牌和细节。
- 节奏：默认轻快，单镜头约 0.8 至 3.5 秒；关键风景允许保留更长镜头。
- 声音：保留少量环境声；有口播时自动降低音乐；没有音乐时可以用环境声组织节奏。
- 字幕：地点、时间、简短感受，不把所有画面都加字幕。
- 用户可调：轻快程度、原声比例、字幕开关、画幅、结尾是否保留风景停留。

### 4.2 电影感短片

“电影感”要拆成可验证的视觉属性，不能只依赖一个模糊标签：

- 镜头：更少的切换、更长的停留、更多前后景关系和有意的留白。
- 节奏：通过情绪曲线推进，而不是每个镜头都卡在鼓点上。
- 声音：环境声和低密度音乐优先，允许在转折处留出安静。
- 画面：统一色彩方向、控制高光和肤色、减少无意义的转场。
- 字幕：少量、克制，除非用户明确要求，不默认铺满画面。
- 预设名称：使用“电影感”“低饱和纪实”“暖色留白”等可描述属性，不在产品内承诺复刻某一位具体导演或现成作品。

### 4.3 快节奏 / 卡点

- 前置条件：必须先取得音乐的节拍网格；没有可靠节拍时，不能声称已经卡点。
- 镜头：动作起点、动作落点和画面变化优先落在节拍附近。
- 节奏：短镜头为主，但要设置最短可辨识时长，避免切到无法阅读或无法看清的程度。
- 声音：音乐为主，口播和环境声按重要性自动闪避；鼓点不能盖住关键台词。
- 用户可调：卡点强度、每拍切换或每两拍切换、是否允许提前或延后若干帧。

### 4.4 家庭纪录片

- 故事：按时间或事件自然推进，优先保留人物关系和完整动作。
- 镜头：人物身份、表情、互动和现场声音比视觉炫技更重要。
- 节奏：允许停顿和不完美，避免过度快切、强烈变速和夸张转场。
- 声音：原声优先，音乐只做托底；孩子或家人说话时自动压低音乐。
- 字幕：只在口播难以听清、需要标注日期地点或用户明确要求时出现。
- 记忆方向：用户对某个人物、某类活动或某种声音的保留偏好可以保存，但人物识别数据本身不能直接写入长期记忆。

### 4.5 口播 / 讲解

- 先以转写结果为主线，再决定画面如何覆盖台词。
- 自动移除长停顿、明显废话和重复句时，必须保留可审阅的删减建议。
- 根据台词重点生成推近、切换 B-roll、关键词字幕和章节标记。
- 语音生成为可选能力：可以生成开场、过渡、补录和多语言版本，但不能默认替换原声。
- 字幕应当与语音时间对齐，支持用户改文案后重新生成时间点。

### 4.6 情绪化蒙太奇

- 先按人物、地点、动作、色彩和时间建立主题线索。
- 镜头不一定按拍摄时间顺序排列，但必须让观众能感知情绪递进。
- 允许重复出现同一人物或意象，但重复应当服务于情绪，而不是素材去重失败。
- 音乐和环境声共同形成情绪曲线；高潮前可以收声，高潮后保留余韵。
- 用户可以指定主题，例如“告别”“成长”“重逢”，Agent 再把主题转成镜头选择条件。

### 4.7 日常生活短片

- 适合素材证据不完整的场景，优先使用文件元数据、轻量画面分析和简单时间顺序。
- 不为了看起来“聪明”而编造人物、地点或故事。
- 以少量剪切、自然原声、简单字幕和轻微音乐完成稳定成片。
- 这是首个 MVP 的兜底风格，也可以作为模型分析失败时的降级策略。

### 4.8 产品 / 功能展示

- 使用建立镜头、功能特写、操作过程和结果对比组织内容。
- 支持屏幕录制、界面截图、字幕指引、局部推近和节奏化标注。
- 这是 LunaAiCut 自身演示视频最适合的风格，也可以用于预告片和更新说明。

## 5. Agent 如何理解用户需求

### 5.1 不让宿主猜意图，让模型产出创作简报

用户输入必须原样发送给 Harness。宿主不得通过关键词、正则、固定短语或字符串包含关系判断用户是在咨询、确认、执行、修改还是结束。

由主 Agent 结合完整对话、项目规则和记忆，生成内部的 `CreativeBrief`。这是模型的结构化工作结果，不是宿主的第二套意图识别器。

```ts
interface CreativeBrief {
  goal: 'create' | 'revise' | 'explore' | 'explain'
  videoType?: string
  subject?: string
  targetDurationSeconds?: number
  aspectRatio?: string
  platform?: 'short-video' | 'landscape' | 'square' | 'unknown'
  pace?: 'slow' | 'balanced' | 'fast' | 'beat-synced'
  emotion?: string[]
  visualDirection?: string[]
  audioDirection?: string[]
  captionDirection?: string[]
  constraints?: string[]
  candidateStyles: Array<{
    styleId: string
    reason: string
    confidence: number
  }>
  unresolvedQuestions: string[]
}
```

例如用户说“帮我剪一个温柔一点的孩子生日视频”，模型应当理解为：视频类型倾向家庭纪录片，情绪倾向温柔，主题是生日，人物可能是孩子和家人，原声优先，可能需要用户补充时长和画幅。宿主只负责校验这个结构是否符合 Schema，不解释“温柔”这个词，也不自行切换工作流。

### 5.2 风格选择不是单选，而是候选与组合

Agent 应当先生成风格候选，再决定是使用一个预设还是组合多个预设：

```text
家庭纪录片 70%
情绪化蒙太奇 20%
电影感短片 10%
```

最终执行时不把百分比直接当成“模型自信度”展示给用户，而是将其转换为可理解的方案：

```text
主体采用“家庭纪录片”，收尾借用“情绪化蒙太奇”的音乐和留白规则。
保留人物原声，字幕只标注日期和关键话语。
```

风格组合必须有冲突解决规则。例如：

- “快节奏”与“家庭纪录片”冲突时，快节奏只影响开场和过渡，不能破坏人物完整动作。
- “口播”与“卡点”同时存在时，台词时间优先，音乐节拍服从台词。
- “电影感”与“字幕密集”同时存在时，字幕密度取用户本轮明确要求，风格只提供默认值。

### 5.3 什么时候需要问用户

Agent 只在缺失的信息会实质改变成片时追问，例如：

- 目标时长会决定镜头数量，但用户完全没有提供。
- 用户要求“配音”，却没有说明使用自己的声音、系统音色还是生成的音色。
- 用户说“用这首歌”，但项目内有多首候选且无法确定是哪一首。
- 用户要求某种人物或情绪，但当前素材没有足够分析证据。

其他情况可以使用风格默认值，并把实际采用的参数展示在创作方案中。不要为了形式上的确认让用户重复输入已经明确表达的要求。

## 6. 基于当前 Harness 的多 Agent 分工

多 Agent 的目标是并行收集证据和提出方案，不是让多个 Agent 同时修改同一条时间轴。

### 6.1 推荐角色

| Agent | 职责 | 能力范围 | 是否修改时间轴 |
| --- | --- | --- | --- |
| Director | 读取用户要求、项目规则和记忆，产出 `CreativeBrief`，选择风格并决定最终方案 | 全局只读；最后调用编辑工具 | 是，唯一的最终执行者 |
| Asset Scout | 批量读取素材元数据、画面证据、字幕和分析状态 | `media.*` 只读 | 否 |
| Story Planner | 根据素材证据和风格生成镜头角色、故事顺序和候选时间段 | 读取素材证据，输出结构化 `ShotPlan` | 否 |
| Audio Planner | 分析音乐、口播、环境声、节拍、响度和闪避方案 | 音频分析工具 | 否 |
| Caption Designer | 设计字幕分段、文本层级、样式和安全区 | 转写与字幕工具只读或生成草案 | 否 |
| Style Critic | 对已生成的方案或预览结果做风格符合度检查 | 读取 `EditPlan`、预览证据和质量指标 | 否 |

Harness 当前已经有 `ctx.agents`、Agent preset、`dsh-agent-loop` 和子 Agent 相关包，可以在现有插件注入点上增加这些角色。第一版不需要为每个用户请求都启动全部角色：由主 Agent 根据已有结构化信息决定是否需要更多分析，宿主只提供资源上限、超时和取消能力。

### 6.2 协作协议

每个只读 Agent 返回结构化报告，不返回只有形容词的散文：

```ts
interface EvidenceReport {
  reportId: string
  sourceMediaIds: string[]
  observations: Array<{
    kind: 'shot' | 'subject' | 'scene' | 'speech' | 'audio' | 'quality'
    mediaId: string
    startSeconds?: number
    endSeconds?: number
    value: unknown
    confidence: number
    evidenceRef: string
  }>
  missingEvidence: string[]
}
```

协作顺序：

1. Director 读取本轮用户原文、项目规则、已有用户偏好和项目概况。
2. Asset Scout、Audio Planner 等只读 Agent 并行收集证据。
3. Director 汇总证据，生成 `CreativeBrief` 和风格候选。
4. Story Planner、Caption Designer 分别生成 `ShotPlan`、`CaptionPlan` 和 `AudioPlan`。
5. Director 将它们合成为 `EditPlan`，调用现有 `timeline.*` 工具或新的批量计划工具执行。
6. Style Critic 读取执行结果或预览结果，提出结构化修改建议。
7. Director 决定是否继续修改，并把结果返回用户。

子 Agent 必须使用作用域工具白名单。只读角色不应拥有 `timeline.*` 写入能力，也不应拥有 `memory.update`。所有写操作串行经过 Director，避免并发 Agent 互相覆盖。

## 7. 需要补齐的工具

### 7.1 P0：让风格预设真正能执行

| 建议工具 | 作用 | 说明 |
| --- | --- | --- |
| `style.list` | 列出内置、项目和用户可用风格 | 返回风格 ID、版本、适用视频类型、依赖的分析能力 |
| `style.inspect` | 读取一个风格的完整声明 | 返回结构化参数和冲突规则，不返回不可控的执行代码 |
| `style.draft` | 将 Agent 生成的创作方向转换成风格草案 | 结果可在 UI 中预览和调整 |
| `style.apply` | 将风格参数绑定到当前创作任务 | 不直接修改时间轴，只建立当前任务的有效风格配置 |
| `style.save` | 保存用户确认的自定义风格 | 支持 `global`、`video-type`、`project` 三种作用域 |
| `style.preview` | 使用小范围素材生成风格预览方案 | 返回镜头、节奏、声音和字幕的可读摘要 |
| `edit.plan` | 将 `CreativeBrief`、风格和证据合成为结构化编辑计划 | 计划中包含前置条件、镜头序列、音频和字幕草案 |
| `edit.apply_plan` | 一次执行已校验的编辑计划 | 第一版可以内部复用已有 `timeline.*`，不要求一次性替换全部工具 |
| `edit.inspect_plan` | 读取计划和执行结果 | 让用户能看到 AI 当前采用了什么风格和哪些素材 |
| `edit.diff` | 返回本次编辑涉及的片段、文字、音频和转场变化 | 这是“AI 做了什么”的解释能力，不是 Git diff |

`edit.apply_plan` 的最低职责是参数校验、调用现有时间轴工具、保存和返回每一步的结构化结果。回滚只需要作为失败时的底层保护，不需要设计成用户必须理解的产品流程。

### 7.2 P0：素材理解能力

现有 `media.analyze` 的 `transcript` 和 `visual` 只够支撑初版，后续需要扩展分析类型：

| 分析类型 | 输出 |
| --- | --- |
| `shot` | 镜头边界、镜头时长、固定/运动、景别、画面变化强度 |
| `scene` | 地点、室内/室外、天气、时间、活动和场景连续性 |
| `subject` | 人物、主体类别、动作、视线、画面占比和可用性 |
| `person` | 可选的人物聚类和用户确认的人物标签；默认不启用跨项目身份识别 |
| `quality` | 模糊、过曝、欠曝、抖动、闭眼、遮挡、无效帧和音频异常 |
| `audio` | 波形、响度、静音段、语音段、音乐段和噪声概况 |
| `beat` | BPM、节拍网格、强拍、段落、起伏和可用于卡点的时间点 |

建议把这些结果保存为素材分析索引，Agent 读取的是有限的结构化证据，不是把整段视频直接塞进上下文。分析任务必须支持取消、重复执行、版本标识和结果失效检测。

### 7.3 P0：声音、字幕和配音

| 建议工具 | 作用 |
| --- | --- |
| `audio.analyze` | 读取音乐和原声的响度、静音、节拍、BPM 和段落 |
| `audio.find_beats` | 返回指定范围内的可用节拍点和置信度 |
| `subtitle.generate` | 根据转写生成字幕分段、标点、时间和关键词 |
| `subtitle.apply_style` | 为字幕应用风格预设中的位置、字号、颜色和动画 |
| `voice.list` | 列出可用本地音色、语言和授权信息 |
| `voice.synthesize` | 根据用户确认的文本和音色生成语音文件 |
| `voice.preview` | 只生成短试听，不直接写入时间轴 |
| `timeline.add_voiceover` | 把已经生成并校验的配音放入时间轴，支持与字幕对齐 |
| `timeline.duck_audio` | 在口播、对白或重要原声期间压低音乐 |

配音相关工具必须把文本、音色、语言、生成模型版本和授权信息作为结构化字段保存。不能因为用户说“温柔一点”就由宿主偷偷切换音色；应由模型选择音色并通过工具参数表达。

### 7.4 P1：用户可以感知的协同能力

| 建议工具 | 作用 |
| --- | --- |
| `variant.create` | 从当前工程创建一个风格版本，例如“旅行 Vlog”和“电影感”两个版本 |
| `variant.list` | 查看同一批素材生成的不同版本 |
| `variant.switch` | 切换当前审阅的版本 |
| `timeline.replace_media` | 用用户指定或 Agent 选择的新镜头替换当前片段，并保持相关属性 |
| `timeline.ripple_edit` | 对连续片段进行插入、删除、闭合空隙和后续片段顺移 |
| `timeline.add_marker` | 标记故事节点、节拍点、重要台词和用户喜欢的镜头 |
| `render.preview` | 输出低分辨率短预览，供 Agent 和用户审阅 |
| `render.inspect` | 返回黑帧、空隙、音频削波、字幕越界和总时长等客观检查结果 |

### 7.5 暂不做成第一版承诺的能力

- 完全自动理解“情绪”并保证剪辑结果符合所有人的审美。
- 只凭文件名准确识别人、地点和故事关系。
- 自动生成有版权风险的音乐并保证可发布。
- 默认跨项目保存脸部特征、声音特征或原始素材内容。
- 通过一个固定提示词实现所有风格，而没有分析证据和可检查参数。

## 8. 开源能力与模型接入路线

### 8.1 总体原则

DeepSeek 继续负责自然语言理解、创作决策和多轮协作；视频分析、转写、配音、节拍和部分视觉能力优先使用本地开源模型或开源框架。

接入方式统一经过现有 `electron/modelLoader.ts` 和通用资源下载器：

- 优先使用 ONNX Runtime 和可替换的模型适配器。
- 模型代码、权重和训练数据相关许可分别核对，不把“代码开源”当作“权重可以发布”。
- 每个模型记录官方来源、版本、许可证、SHA256 和模型用途。
- 客户端下载源只使用 ModelScope 和 Luna GitCode resource release，多个源必须是相同文件、相同大小和相同 SHA256。
- 不把模型二进制提交到仓库或打入安装包。
- 模型运行时、模型文件和风格预设都要有版本号，以便分析结果失效时重新计算。

### 8.2 建议的开源技术候选

下面是能力方向，不代表立即锁定具体模型。实现前必须按当前项目的开源、非商业用途和再分发要求逐项审计。

| 能力 | 第一阶段候选 | 作用 | 接入方式 |
| --- | --- | --- | --- |
| 语音转写 | `whisper.cpp`、`faster-whisper`、FunASR 系列 | 生成口播、对白、字幕和搜索证据 | 优先寻找可稳定转换为 ONNX 的模型；通过本地分析服务输出时间段 |
| 文本生成语音 | CosyVoice、GPT-SoVITS、OpenVoice 等候选 | 生成开场、补录、过渡和多语言配音 | 先做短试听和文件生成，再接入 `timeline.add_voiceover` |
| 镜头切分 | PySceneDetect，结合视频帧差和本地视觉模型 | 识别镜头边界和场景变化 | Python 或 Node worker 输出统一 `ShotObservation` |
| 视觉理解 | Qwen 系列视觉模型、LLaVA 系列等候选 | 场景、动作、主体、画面描述 | 只返回时间点证据和置信度，不把未观察内容写成事实 |
| 人物与主体 | 人脸检测、人体检测和可选聚类模型 | 找到人物出现区间、主体位置和连续性 | 默认只做当前项目内的匿名聚类，跨项目身份需要用户明确开启 |
| 画面质量 | OpenCV、帧级亮度/清晰度/抖动指标 | 排除废片、黑帧、过曝和不可用片段 | 作为 `quality` 证据，不直接决定删除 |
| 音频分析 | FFmpeg、librosa、Web Audio 分析能力 | 波形、响度、静音、BPM 和节拍 | 音频特征保存在分析缓存，卡点工具读取节拍网格 |
| 向量检索 | ONNX Embedding 模型或 OpenCLIP 候选 | 按“海边”“孩子奔跑”“夕阳”等主题检索镜头 | 向量只服务当前项目或用户明确允许的范围 |
| 推理运行时 | ONNX Runtime，必要时补充 `llama.cpp` 等适配器 | 统一本地推理生命周期 | 通过模型清单和平台资源目录管理 |
| 媒体处理 | FFmpeg | 抽帧、代理、音频分离、预览和导出 | 复用项目已有媒体和导出边界 |

语音生成需要单独规划，因为它涉及声音授权、声音克隆、语言覆盖、模型大小和平台性能。第一版可以先支持“从文本生成一段本地配音并放进时间轴”，再做声音克隆和连续角色音色。

### 8.3 TTS 的分阶段实现

1. `voice.list` 只列出已安装且许可证信息完整的本地音色。
2. `voice.preview` 生成 5 至 10 秒试听，用户明确选择后才进入创作方案。
3. `voice.synthesize` 生成带元数据的音频文件，记录文本、模型、音色、语言、采样率和校验值。
4. `timeline.add_voiceover` 放入时间轴，支持从文本时间点生成字幕。
5. 通过 `timeline.duck_audio` 处理背景音乐和原声。
6. 用户修改文本时只重新生成受影响的片段，不重新生成整条视频。

声音克隆放到后续阶段，并且必须有明确的本地授权确认、模型限制和可删除的生成记录。

## 9. 用户自定义风格和扩展

### 9.1 三种扩展方式

#### 方式一：界面化风格编辑器

面向普通用户，提供以下控件：

- 节奏：慢、平衡、快、卡点强度。
- 画面：自然、明亮、低饱和、暖色、冷色、运动强度。
- 镜头：人物优先、风景优先、动作优先、细节优先。
- 声音：原声比例、音乐比例、人声优先、环境声保留。
- 字幕：关闭、重点字幕、全量字幕、样式和安全区。
- 叙事：按时间、按地点、按人物、按情绪、按主题。

用户修改控件后，模型把它们合成为一个可保存的结构化风格。界面不要求用户理解 JSON、模型或工具协议。

#### 方式二：自然语言生成风格草案

用户可以说：

> 我想要一种安静、留白多、保留环境声、字幕很少的家庭视频风格。

Agent 生成风格草案，展示“节奏、镜头、声音、字幕”的实际参数，用户确认后才能保存为个人风格。一次对话中的临时描述不自动变成长期风格。

#### 方式三：导入声明式风格包

高级用户可以导入 JSON/YAML 风格包。第一版只允许：

- 风格元数据和版本。
- 参数默认值和可调范围。
- 镜头角色和故事结构。
- 音频、字幕、画面和质量规则。
- 所需分析能力和模型版本范围。
- 作者、许可证和来源。

第一版禁止风格包执行任意代码、读取任意路径、访问网络或调用未声明工具。需要扩展工具时，应通过 Harness plugin 和明确的工具 Schema 接入，而不是在用户风格文件里偷偷执行代码。

### 9.2 风格与记忆的边界

- 用户保存的“我的家庭视频风格”是风格资产。
- 用户确认的“以后字幕都靠下、不要挡脸”是长期偏好。
- 当前任务的“这一条做 30 秒、用这首歌”只是本轮要求。
- 项目 `AGENTS.md` 中的剪辑原则是项目规则。

四者不能互相覆盖，也不能通过自然语言字符串匹配来决定写入哪里。

## 10. “AI 记住你”的实现方式

记忆先从偏好开始，不从自动学习复杂模型开始。

### 10.1 适合保存的内容

- 字幕位置、密度和常用样式。
- 常用画幅和目标平台。
- 旅行、家庭、口播等视频类型的节奏偏好。
- 音乐与原声的相对优先级。
- 用户经常保留或拒绝的镜头类型。
- 用户对风格草案的确认和修正。

### 10.2 不应直接保存的内容

- 原始视频和音频。
- 完整聊天记录。
- 没有用户确认的人物身份。
- 默认跨项目使用的人脸 embedding、声纹或敏感人物信息。
- 一次性时长、当前素材选择和当前项目专属安排。

### 10.3 从用户反馈中学习

用户说“这个镜头我更喜欢”时，当前工程可以记录一个项目级反馈，用于当前任务后续修改。只有用户明确确认“以后都按这个习惯”时，主 Agent 才调用 `memory.update` 保存跨项目偏好。

记忆条目应包含：作用域、视频类型、主题、偏好内容、来源说明、确认状态、更新时间和版本。模型读取记忆时只把它当作默认倾向，不能覆盖本轮明确要求。

## 11. 分阶段开发计划

以下顺序以“先让风格能用，再让风格更懂素材，最后开放用户扩展”为准。

### 阶段 0：风格协议和创作简报

目标：让 Agent 能稳定理解用户要求并产出结构化风格方案。

工作内容：

- 定义 `CreativeBrief`、`StyleManifest`、`ShotPlan`、`AudioPlan`、`CaptionPlan` 和 `EditPlan`。
- 增加 `style.list`、`style.inspect`、`style.draft`、`style.preview`。
- 把当前项目规则、用户记忆和本轮要求接入风格决策链。
- 保持用户消息原样传递，删除任何基于文案的宿主分类逻辑。
- 为每个内置风格建立最小测试素材和预期结构。

完成标准：

- 同一批素材可以生成至少 3 个结构明显不同的风格方案。
- 用户说“快一点”“字幕少一点”“保留原声”时，方案只修改对应参数。
- 方案中每个镜头选择都能指向素材 ID 和时间证据。
- 没有素材证据时，Agent 明确说明缺口，不伪造理解结果。

### 阶段 1：风格预设 MVP，直接完成基础剪辑

目标：先用现有工具兑现“帮我把素材剪成一条 30 秒旅行视频”。

工作内容：

- 完成旅行 Vlog、家庭纪录片、口播、电影感、快节奏、情绪蒙太奇 6 个预设。
- 用现有 `media.*` 和 `timeline.*` 工具完成镜头选择、加入、裁剪、重排、字幕和基础音量调整。
- 增加 `edit.plan` 和 `edit.apply_plan`，内部编排现有工具。
- 增加 `edit.diff`，让用户能看到本次采用的风格和修改范围。
- 使用现有 Harness 子 Agent 机制加入 Asset Scout 和 Story Planner，只读并行。

完成标准：

- 同一批素材分别生成“旅行 Vlog”和“家庭纪录片”时，镜头顺序、镜头时长、字幕密度和声音策略有可验证差异。
- 多轮对话中，用户要求只调整风格时，不会无故重建全部工程。
- 现有 `project-source-ai-tools.test.ts`、Harness 回放测试和 Playwright Electron E2E 能验证工具结果回传、时间轴结果和重启恢复。

### 阶段 2：素材理解增强

目标：让 Agent 从“读取已有抽帧描述”升级为“按镜头、人物、场景和质量找素材”。

工作内容：

- 增加镜头边界和故事板索引。
- 增加场景、主体、人物出现区间和画面质量证据。
- 增加当前项目内的语义检索，例如“找海边”“找孩子吹蜡烛”“找所有人一起出现的镜头”。
- 建立分析版本、缓存、取消和失败重试协议。
- 将长素材分析放入可观察的后台任务，不阻塞编辑器交互。

完成标准：

- 几百个素材可以分页列出、按证据检索和分批分析。
- Agent 不再依赖文件名来决定镜头内容。
- 分析结果缺失、过期或置信度不足时，会回到补充分析或询问用户，而不是继续编造。

### 阶段 3：声音、字幕、节拍和 TTS

目标：兑现“添加字幕、根据音乐卡点、生成配音”的可控版本。

工作内容：

- 接入转写、分句、字幕样式和字幕时间对齐。
- 接入波形、响度、静音和节拍分析。
- 增加卡点强度和台词优先级规则。
- 接入首个本地开源 TTS 适配器，先支持试听、生成和放入时间轴。
- 增加背景音乐闪避和人声混音工具。

完成标准：

- 用户可以要求“保留人声、音乐弱一点、字幕跟着口播”，Agent 可以生成可审阅结果。
- 只有在节拍分析成功且置信度足够时，才把结果描述为卡点。
- 修改配音文本时只重新生成受影响段落。
- 所有模型和音色都能展示来源、版本和授权信息。

### 阶段 4：风格组合、版本和用户自定义

目标：让用户可以基于同一批素材得到多个不同作品，并保存自己的方法。

工作内容：

- 增加 `variant.*`，支持不同风格版本并存。
- 上线界面化风格编辑器。
- 支持自然语言生成风格草案，用户确认后保存。
- 支持声明式 JSON/YAML 风格包导入导出。
- 增加项目级风格、视频类型风格和全局风格的优先级管理。

完成标准：

- 用户可以从同一个工程生成旅行、电影感和家庭纪录片三个版本。
- 切换版本不会破坏原工程，也不会把一次性要求写入长期记忆。
- 自定义风格包无法调用未声明的工具、读取未授权路径或执行任意代码。

### 阶段 5：偏好学习和多 Agent 优化

目标：让“还是按照我的风格来”逐渐变成可靠的默认值。

工作内容：

- 建立用户确认的长期偏好和项目反馈的区别。
- 让 Style Critic 根据客观指标和用户反馈提出改进建议。
- 增加风格偏好对比，例如用户连续选择更长镜头或更少字幕后形成待确认建议。
- 对复杂任务启用 Asset Scout、Audio Planner、Caption Designer 和 Style Critic。
- 为每个 Agent 限制工具范围、上下文大小、并发量和取消边界。

完成标准：

- 用户确认的偏好可以在下一次同类型项目中生效。
- 没有确认的候选偏好不会自动污染全局记忆。
- 子 Agent 之间只通过结构化报告协作，工具结果仍回到主 Agent，由主 Agent 决定下一步。

## 12. 下一阶段开发计划

下一阶段的目标不是继续增加提示词数量，而是让当前内置技能可以稳定地产出方案、执行时间轴修改，并且能被测试验证。建议按以下里程碑推进。

### 12.1 里程碑 A：风格协议和注册表

目标：把当前 Markdown 提示词与结构化风格参数对应起来，让 Agent 和后续工具有稳定的输入。

工作内容：

- 定义 `CreativeBrief`、`StyleManifest`、`ShotPlan`、`AudioPlan`、`CaptionPlan` 和 `EditPlan` 类型及 Schema。
- 给 6 个内置风格补充版本、适用视频类型、默认参数、依赖的分析能力和质量检查项。
- 增加 `style.list` 和 `style.inspect`，先只读，不修改时间轴。
- 规定风格冲突合并顺序：本轮用户要求 > 项目规则 > 用户长期偏好 > 风格默认值。
- 将风格选择和参数合并完全交给模型，宿主只做 Schema、权限和数据边界校验。

验收：同一批素材可以得到至少 3 个结构不同的风格方案；每个方案的镜头、节奏、声音和字幕规则都有结构化字段；不存在素材证据时方案能明确标记缺口。

### 12.2 里程碑 B：基础剪辑执行闭环

目标：兑现第一句真实用户需求，例如“帮我把这些素材剪成一条 30 秒旅行视频”。

工作内容：

- 增加 `style.draft` 和 `style.preview`，让用户先看到采用的风格与创作方案。
- 增加 `edit.plan`、`edit.apply_plan` 和 `edit.diff`，内部复用现有 `media.*`、`timeline.*` 工具。
- 先实现旅行 Vlog、家庭纪录片和电影感三个验收样例，再补快节奏、口播和情绪蒙太奇。
- 每次批量编辑后重新检查时间轴，保留结构化工具结果并交回模型继续判断。
- 让用户可以继续说“这里慢一点”“这个镜头我更喜欢”“字幕少一点”，只修改受影响部分。

验收：同一批素材生成旅行 Vlog、家庭纪录片和电影感三个结果时，镜头顺序、镜头时长、字幕密度和音频策略有可验证差异；多轮修改不会无故重建整个时间轴。

### 12.3 里程碑 C：素材证据增强

目标：让风格选择从“读取已有描述”升级为“按镜头、场景、主体和质量找素材”。

工作内容：

- 先实现镜头切分、波形、响度、静音段和基础画面质量分析。
- 统一分析结果的版本、时间范围、置信度、证据引用、取消和过期规则。
- 再增加场景、主体、人物匿名聚类和当前项目内语义检索。
- 所有分析结果保存为有限的结构化索引，不把整段视频直接塞进模型上下文。

验收：几百个素材可以分批分析、分页读取和按证据检索；分析缺失或过期时 Agent 会补充分析或说明缺口，不依赖文件名编造内容。

### 12.4 里程碑 D：字幕、节拍和 TTS

目标：逐步兑现预告片中的“添加字幕、音乐卡点和生成配音”。

工作内容：

- 先完成转写分句、字幕时间对齐和字幕样式应用。
- 接入音频分析和节拍网格；只有节拍置信度足够时才允许报告“已卡点”。
- 接入首个本地开源 TTS，先做 `voice.list`、`voice.preview`、`voice.synthesize` 和 `timeline.add_voiceover`。
- 保存文本、音色、语言、模型版本、许可证和文件校验值。
- 增加人声优先、音乐闪避和环境声保留规则。

验收：用户可以先试听配音，再确认放入时间轴；修改配音文本时只重新生成受影响片段；模型不会把尚未完成的节拍分析或 TTS 生成说成已完成。

### 12.5 里程碑 E：风格版本和用户扩展

目标：让同一批素材可以产生多个作品，并允许用户保存自己的剪辑方法。

工作内容：

- 增加 `variant.create`、`variant.list` 和 `variant.switch`。
- 提供界面化风格编辑器和自然语言生成风格草案。
- 支持声明式 JSON/YAML 风格包导入导出，禁止任意代码、Shell、网络和未声明工具。
- 区分项目风格、视频类型风格、全局风格和长期记忆的作用域与优先级。

验收：用户可以从同一个工程生成并切换旅行、电影感和家庭纪录片三个版本；自定义风格不能破坏原工程，也不能越权读取文件或调用工具。

### 12.6 里程碑 F：多 Agent 和偏好学习

目标：在工具和数据协议稳定后，再引入并行分析和“越来越懂你”。

工作内容：

- 增加只读 Asset Scout、Audio Planner、Caption Designer 和 Style Critic。
- 只读 Agent 通过结构化报告协作，所有时间轴写操作仍由 Director 串行执行。
- 将用户明确确认的长期偏好与一次性项目反馈分开保存。
- 为并发量、超时、取消、上下文大小和工具白名单建立资源边界。

验收：子 Agent 不能互相覆盖时间轴；没有用户确认的候选偏好不会写入全局记忆；复杂任务失败时可以退回单 Agent 流程。

### 12.7 近期实际执行顺序

1. 在 `packages/freecut-editor/src/features/project-source/` 新增风格清单、风格 Schema 和创作简报类型。
2. 将内置 6 个风格写成声明式配置，并为每个风格添加可验证的镜头、节奏、声音和字幕规则。
3. 在 `scripts/deepseek-harness-freecut-plugin.mjs` 注册 `style.*` 和 `edit.*` 的稳定工具描述。
4. 在 `project-source` 工具层实现风格读取、方案预览和编辑计划编排。
5. 把当前 `media.analyze` 的输出统一成带版本和证据引用的分析结果。
6. 先实现镜头切分、波形和节拍分析，再引入人物和语义检索，避免一开始就把所有模型耦合到 Agent。
7. 接入字幕生成和 `timeline.add_voiceover` 所需的时间轴数据结构。
8. 选择一个开源 TTS 适配器做本地试听闭环，完成许可证、模型资源和 SHA256 记录。
9. 用 Harness 的 `ctx.agents` 创建只读 Asset Scout 和 Audio Planner preset，验证多 Agent 报告汇总。
10. 做风格版本和用户自定义风格包，最后再把用户反馈转成长期记忆候选。

## 13. 验收与测试重点

### 逻辑和协议测试

- `CreativeBrief`、`StyleManifest`、`EditPlan` 的 Schema 校验。
- 风格组合的参数冲突和优先级。
- 工具结果始终返回模型，模型可以继续调用工具或给出答复。
- 用户消息保持原文，不在宿主层增加“已确认”“开始执行”等推断文本。
- 分析任务可取消、重复调用不会生成错误的重复索引。
- TTS 生成结果包含文本、音色、模型版本、许可信息和文件校验值。
- 自定义风格包只能使用声明的能力，不能执行任意代码。

### 模型回放测试

至少准备以下固定场景：

- “帮我做一条 30 秒旅行 Vlog”。
- “同样素材再做一个电影感版本”。
- “保持画面不变，只把节奏快一点”。
- “这是孩子生日视频，保留原声，字幕少一点”。
- “根据这首音乐卡点，但不要盖住口播”。
- “生成一段温柔的开场配音，先试听”。

测试断言应检查结构化的风格选择、工具序列、素材证据和时间轴结果，不要为某个具体中文短语建立宿主意图分类测试。

### Electron E2E

里程碑阶段使用仓库现有的 Playwright Electron fixture，覆盖：

- 打开项目、打开剪辑助手、提交创作要求。
- 风格方案显示并执行基础时间轴修改。
- 同一项目生成两个风格版本并切换。
- 素材分析过程中取消，编辑器仍可继续使用。
- TTS 试听、确认后生成音频并出现在时间轴。
- 重启后保留已确认的风格和已生成的时间轴。
- fixture 收集的 renderer `pageerror` 和 error console 为空。

## 14. 最终产品判断标准

这个方向是否成立，不看 Agent 能说多少漂亮的话，而看以下四件事：

1. 同一批素材换一个风格，作品结构是否真的不同。
2. 用户说“这里慢一点”“这个镜头我更喜欢”“字幕少一点”后，Agent 是否只修改对应部分。
3. Agent 是否能说明每个选择依据了哪些素材证据，而不是假装看过素材。
4. 用户是否可以从预设开始，逐步调成自己的风格，并在之后复用。

LunaAiCut 的产品定位应当是：DeepSeek 负责理解和创作决策，开源模型负责素材与声音证据，风格技能负责把审美变成结构化策略，时间轴工具负责把策略变成用户可以继续接管的真实工程。

这四部分协同起来，才是预告片里“真正懂自己的 AI 剪辑师”，而不是一个会描述剪辑方法的聊天机器人。
