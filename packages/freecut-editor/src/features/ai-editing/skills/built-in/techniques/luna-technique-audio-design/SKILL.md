---
name: luna-technique-audio-design
description: 声音设计与混音技能。用户要求配音、背景音乐、音效、对白清晰、环境声、声音桥接、淡入淡出或音乐压低时使用；通过已有音频工具和时间轴工具组织声音层级。
---

# 声音设计与混音

## 声音优先级

- 口播、对白和关键现场声音优先于音乐。
- 环境声用于交代地点、空间和真实感；不要无理由全部静音。
- 音乐提供情绪和段落推动，不能盖住信息。
- 音效只在动作、转场或重点出现时使用，避免连续堆叠。

## 生成与加入素材

1. 先用 `luna.media.list` 确认已有音频，用 `luna.media.read` 或 `luna.media.analyze(kind=transcript)` 获取对白证据；提交分析后用 `luna.media.getAnalysisTask` 查询到完成，再读取字幕。
2. 需要配音时调用 `luna.audio.startSpeech`，需要背景音乐时调用 `luna.audio.startMusic`；两者只提交后台任务，必须用 `luna.audio.getTask` 查询到 `completed` 后再使用 `mediaId`。
3. 音频任务完成后，模型根据用户目标决定是否继续调用 `luna.timeline.addMedia`，不要把生成和入轴伪装成一个宿主工作流。
4. 用 `luna.timeline.setAudio` 调音量、淡入、淡出和必要的音高；每次修改后检查时间轴。

## 常用声音结构

- **对白主导**：对白开始前保留少量环境声；对白中音乐降低；句尾保留自然尾音。
- **声音桥接**：下一场的环境声或音乐先进入，上一场声音延后结束，让空间变化更顺滑。
- **音乐主导**：按段落而不是每拍切换；高潮前留出上升空间，高潮后允许回落。
- **氛围主导**：降低音乐密度，保留风、脚步、街道或房间底噪作为记忆线索。

## 约束

- 没有可靠转写时不要把听不清的内容写成字幕或配音文案。
- 没有节拍分析结果时可以设计段落节奏，但不能宣称已经自动卡点。
- 不支持的降噪、母带、自动闪避或声道修复不能声称已完成；可以在方案中标明需要用户手工处理。
