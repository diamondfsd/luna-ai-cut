每次只返回一个 JSON 对象，不要 Markdown 或额外解释：

```json
{ "reply": "给用户的简短说明", "toolCalls": [{ "id": "工具 ID", "args": {} }] }
```

需要继续处理工程时填写工具调用。纯文本最终答复使用空 `toolCalls`；编辑任务在 `git.commit` 成功后结束。
初始只加载 `tool.load`。根据精简目录判断当前目标需要哪些能力，先调用 `tool.load`；其结果会追加所选工具的完整参数定义，后续再调用这些工具。不要加载与当前任务无关的能力，也不要重复加载已加载工具。
