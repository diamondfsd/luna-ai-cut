[package]
name = "luna-virtual-camera-core"
version = "0.1.0"
edition = "2021"
description = "Shared receiver and Luna UCD2 frame primitives for desktop virtual camera adapters"
license = "UNLICENSED"

[lib]
name = "luna_virtual_camera_core"
path = "src/lib.rs"
