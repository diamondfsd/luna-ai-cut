# v1.5.1

## Bug 修复

- **修复部分 macOS 无法连接相机的问题**：macOS 构建改为使用 Ad Hoc 签名，并在 CI 中增加签名校验，提升连接相机 Wi-Fi 热点时访问本地网络的兼容性。

## 改进

- **macOS 发布包增加本地网络访问说明**：保留相机连接所需的网络权限配置。
- **热更新包体积优化**：内置字体和 LUT 由正式安装包提供，避免重复打入热更新包。
