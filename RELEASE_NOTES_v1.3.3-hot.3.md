# v1.3.3-hot.3 — 热更新发布说明

## Bug 修复

- **修复缩略图生成失败时无日志的问题**：`cacheFile()` 中的 `catch { /* 静默 */ }` 改为记录详细错误日志（本地路径检查、下载尝试、失败原因等），方便排查缩略图不显示的问题。

## 改进

- **HTTP URL 和相机路径不再被脱敏**：`sanitizePaths` 现在会保留 `http://` 开头的 URL 和 `/DCIM`、`/storage_` 开头的相机路径完整显示，让日志更能反映真实请求。
